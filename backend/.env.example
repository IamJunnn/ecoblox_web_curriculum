# ========================================
# EcoBlox Academy Backend Environment Variables
# ========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ========================================
# DATABASE CONFIGURATION
# ========================================
# PostgreSQL connection string
# Local development: postgresql://USER:PASSWORD@localhost:5432/ecoblox_academy
# Railway: Will be automatically provided by Railway PostgreSQL service
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# ========================================
# JWT AUTHENTICATION
# ========================================
# Generate a secure random string (32+ characters)
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-CHANGE-THIS
JWT_EXPIRES_IN=24h

# ========================================
# SERVER CONFIGURATION
# ========================================
# Port for the backend server (Railway will override this)
PORT=3400

# Frontend URL for CORS configuration
# Development: http://localhost:3000
# Production: https://your-app.vercel.app
FRONTEND_URL=http://localhost:3000

# ========================================
# EMAIL CONFIGURATION (Optional)
# ========================================
# For sending parent notifications about student progress
# Gmail: Use an app-specific password (not your regular password)
# https://support.google.com/accounts/answer/185833
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password

# ========================================
# PRODUCTION DEPLOYMENT NOTES
# ========================================
# Railway:
# - DATABASE_URL is automatically set by Railway PostgreSQL
# - Set JWT_SECRET to a secure random string
# - Set FRONTEND_URL to your Vercel deployment URL
# - Set PORT (Railway will override automatically)
# - Set EMAIL_USER and EMAIL_PASS if using email notifications
#
# Remember:
# 1. Generate a NEW JWT_SECRET for production
# 2. Never use default/example values in production
# 3. Use environment variables in Railway dashboard
# 4. Keep secrets secure and rotate them periodically
